ENV=local

# --- Database (MySQL, 비동기 — asyncmy) ---
DB_HOST=mysql
DB_PORT=3306
DB_USER=remedi_user
DB_PASSWORD=changeme
DB_NAME=remedi_db
DB_ROOT_PASSWORD=changeme_root

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- ChromaDB (Server Mode) ---
CHROMA_HOST=chroma
CHROMA_PORT=8001

# --- JWT (decision_log.md 2026-07: httpOnly 쿠키 -> JWT) ---
JWT_SECRET_KEY=dev-only-insecure-secret-CHANGE-ME
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_MINUTES=20160

# --- 외부 API ---
MFDS_API_KEY=

# --- Frontend ---
CORS_ORIGINS=["http://localhost:5173"]
